---
layout: layouts/base.liquid
pagination:
  data: collections
  size: 1
  alias: tag
  filter: all
permalink: /posts/tagged/with/{{ tag }}/
---
Posts tagged with <span class="tag">{{tag}}</span>

<ul>
{% assign taglist = collections[ tag ] %}
{% for post in taglist | reverse %}
  <li><a href="{{ post.url }}">{{ post.data.title }}</a></li>
{% endfor %}
</ul>
